[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "zrobot-web"
version = "0.1.0"
description = "ZRobot Web Server with PyO3 bindings"
requires-python = ">=3.9"

[tool.maturin]
features = ["extension-module"]
manifest-path = "Cargo.toml"